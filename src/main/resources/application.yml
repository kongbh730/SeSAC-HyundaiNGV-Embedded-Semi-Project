spring:
  datasource:
    url: jdbc:mysql://localhost:3306/EmbeddedDB  # DB URL 확인
    username: root                         # DB 사용자 이름
    password: MySQLKong730%%                         # DB 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver     # 드라이버 클래스 확인

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    encoding: UTF-8
    cache: false

server:
  port: 8081
  address: 0.0.0.0
